using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class A_click : MonoBehaviour
{
    public static bool a_click_bool = false;
    // Start is called before the first frame update
    void Start()
    {
        a_click_bool = false;
    }
}
